<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!--Error label text style-->
    <style name="EditErrorText" parent="TextAppearance.AppCompat.Small">
        <item name="android:textColor">@color/colorRed</item>
    </style>

    <style name="EditRegTextTheme" parent="Theme.AppCompat.Light">
        <item name="colorControlNormal">@color/colorWhiteOpacity</item>
        <item name="colorControlActivated">@color/colorWhite</item>
    </style>

    <style name="EditHintText" parent="TextAppearance.AppCompat.Small">
        <item name="android:textColor">@color/colorWhiteOpacity</item>
        <item name="android:textSize">@dimen/et_hint_text_size</item>
    </style>

    <style name="ToolbarBackArrowColor" parent="Theme.AppCompat.Light">
        <item name="android:textColorSecondary">@color/colorWhite</item>
    </style>

    <style name="FBButtonStyle">
        <item name="android:paddingLeft">@dimen/button_padding</item>
        <item name="android:paddingRight">@dimen/fb_button_padding</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:textColor">@color/colorWhite</item>
        <item name="android:textColorHint">@color/colorWhite</item>
        <item name="android:textSize">@dimen/fb_btn_font_size</item>
        <item name="android:textStyle">bold</item>
        <item name="android:background">@drawable/button_shape_fbsignup</item>
        <item name="android:drawableStart">@drawable/fb_icon</item>
    </style>

    <style name="CustomProgressBar" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorAccent">@color/colorGreenDark</item>
    </style>
</resources>
